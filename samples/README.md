# Samples

Runnable examples: provisioning templates, pipeline definitions, configuration
files.

Every sample must use `contoso` placeholders — no real tenant names, site URLs,
tenant GUIDs, or app registration IDs. Each sample directory gets a `README.md`
saying what it does, what it assumes, and what it will change if run.
